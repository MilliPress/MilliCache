version: 2
updates:
  - package-ecosystem: "composer"
    directory: "/"
    # Open PRs against the active dev branch, not the default branch (main)
    target-branch: "next"
    schedule:
      interval: "weekly"
    groups:
      # Group all dev dependencies except key packages
      dev-dependencies:
        dependency-type: "development"
        exclude-patterns:
          - "predis/*"
          - "millipress/*"
    # These get individual PRs for careful review
    ignore:
      # Don't group these - we want separate PRs
      - dependency-name: "predis/predis"
        update-types: ["version-update:semver-patch"]
      - dependency-name: "millipress/millirules"
        update-types: ["version-update:semver-patch"]
      # Pest 5+ would require a future PHP bump — ignore when available
      - dependency-name: "pestphp/pest"
        versions: [">=5.0"]

  - package-ecosystem: "npm"
    directory: "/"
    target-branch: "next"
    schedule:
      interval: "monthly"
    groups:
      npm-dependencies:
        patterns:
          - "*"

  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "next"
    schedule:
      interval: "weekly"
