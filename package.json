{
  "name": "millicache",
  "version": "1.0.0-beta.1",
  "author": "Philipp <hello@milli.press>",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "@types/node": "^20.12.7",
    "@wordpress/e2e-test-utils-playwright": "^0.23.0",
    "@wordpress/env": "^9.7.0",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "wp-env": "wp-env",
    "env": "node ./tests/wp-env/dev-server.mjs",
    "env:start": "npm run env start",
    "env:stop": "npm run env stop",
    "env:reset": "wp-env clean all && npm run env:start",
    "env:destroy": "npm run env destroy",
    "env:test": "npx playwright test --headed",
    "env:cli": "wp-env run cli",
    "env:redis-cli": "wp-env run cli bash -c 'redis-cli -h ${1:-redis}'",
    "env:tests-cli": "wp-env run tests-cli $@",
    "env:tests-redis-cli": "wp-env run test-cli bash -c 'redis-cli -h ${1:-redis}'"
  }
}
